import fs from 'node:fs';
export declare const lockOptions: {
    stale: number;
};
export declare const lockRetryOptions: {
    retries: {
        retries: number;
        maxTimeout: number;
        factor: number;
    };
    fs: typeof fs;
    stale: number;
};
