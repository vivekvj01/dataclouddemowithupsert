import { DataCloudApi, DataCloudQuery, DataCloudQueryResponse, DataCloudUpsertResponse } from "../index.js";
export declare class DataCloudApiImpl implements DataCloudApi {
    readonly accessToken: string;
    private readonly domainUrl;
    private request;
    constructor(accessToken: string, domainUrl: string);
    query(sql: DataCloudQuery): Promise<DataCloudQueryResponse>;
    queryNextBatch(nextBatchId: string): Promise<DataCloudQueryResponse>;
    upsert(name: string, objectName: string, data: any): Promise<DataCloudUpsertResponse>;
}
