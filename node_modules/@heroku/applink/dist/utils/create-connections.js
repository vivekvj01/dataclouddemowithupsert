"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createConnection = createConnection;
const connection_js_1 = require("jsforce/lib/connection.js");
function createConnection(options) {
    const { accessToken, instanceUrl, version } = options;
    return new connection_js_1.Connection({
        accessToken,
        instanceUrl,
        version,
        callOptions: {
            client: `salesforce-sdk-nodejs-v1:${process.env.npm_package_version}`,
        },
    });
}
