import { Connection } from "jsforce/lib/connection.js";
export type CreateConnectionOptions = {
    accessToken: string;
    instanceUrl: string;
    version: string;
};
export declare function createConnection(options: CreateConnectionOptions): Connection<import("jsforce/lib/types").Schema>;
