import { ForceApi, Logger, UnitOfWork } from '..';
/**
 * Represents invoking user.
 */
export declare class UserContext {
    readonly orgDomainUrl: string;
    readonly orgId: string;
    readonly salesforceBaseUrl: string;
    readonly username: string;
    readonly userId: string;
    readonly onBehalfOfUserId?: string;
    constructor(orgDomainUrl: string, orgId: string, salesforceBaseUrl: string, username: string, userId: string, onBehalfOfUserId?: string);
}
/**
 * Respresents the context of the function invocation including objects needed by functions to
 * perform their action(s), eg ForceAPI to interact with the invoking org.
 */
export declare class Context {
    readonly apiVersion: string;
    readonly userContext: UserContext;
    readonly logger: Logger;
    readonly payloadVersion: string;
    readonly forceApi?: ForceApi;
    readonly unitOfWork?: UnitOfWork;
    constructor(apiVersion: string, userContext: UserContext, logger: Logger, payloadVersion: string, forceApi?: ForceApi, unitOfWork?: UnitOfWork);
}
